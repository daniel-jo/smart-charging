# --- macOS ------------------------------------------------------------------
.DS_Store

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# --- Local fixtures -----------------------------------------------------------
# This integration has no API keys or secrets. Do NOT commit price fixture
# files with real price data — use .sample files for checked-in examples.
dryrun_prices.json